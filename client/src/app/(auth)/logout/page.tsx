import React from "react";
import Logout from "@/app/components/auth/components/Logout";

const Page = () => {
    return (
        <div>
           <Logout/>
        </div>
    );
};

export default Page;
